<x-admin-layout title="Detail Murid">
    <a href="{{ route('admin.students.index') }}" class="text-sm font-semibold text-sky-700">← Kembali ke Data Murid</a>
    <section class="mt-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="text-2xl font-bold text-slate-900">{{ $student->name }}</h2><p class="mt-1 text-slate-500">{{ $student->email }}</p></div><div class="flex items-center gap-3">@if ($student->studentProfile)<a href="{{ route('student-recaps.export', $student->studentProfile) }}" class="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold text-white">Download Excel</a>@endif<span class="rounded-full bg-slate-100 px-3 py-1 text-sm font-semibold text-slate-700">{{ ucfirst($student->status->value) }}</span></div></div><dl class="mt-7 grid gap-5 sm:grid-cols-2"><div><dt class="text-sm text-slate-500">Nomor HP</dt><dd class="font-semibold">{{ $student->studentProfile?->phone ?? 'Belum diisi' }}</dd></div><div><dt class="text-sm text-slate-500">Jurusan</dt><dd class="font-semibold">{{ $student->major?->name ?? 'Belum diisi' }}</dd></div><div><dt class="text-sm text-slate-500">Tempat PKL</dt><dd class="font-semibold">{{ $student->studentProfile?->pkl_place_name ?? 'Belum diisi' }}</dd></div><div class="sm:col-span-2"><dt class="text-sm text-slate-500">Lokasi PKL</dt><dd><x-pkl-map-link :profile="$student->studentProfile" /></dd></div></dl></section>
</x-admin-layout>
