<x-admin-layout title="Jurusan">
    <div class="mb-6 flex justify-end"><a href="{{ route('admin.majors.create') }}" class="rounded-lg bg-sky-700 px-4 py-2.5 text-sm font-bold text-white hover:bg-sky-800">Tambah jurusan</a></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">Kode</th><th class="px-5 py-4">Nama jurusan</th><th class="px-5 py-4">Pengguna</th><th class="px-5 py-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($majors as $major)<tr><td class="px-5 py-4 font-semibold">{{ $major->code }}</td><td class="px-5 py-4">{{ $major->name }}</td><td class="px-5 py-4 text-slate-600">{{ $major->users_count }}</td><td class="px-5 py-4"><div class="flex justify-end gap-3"><a href="{{ route('admin.majors.edit', $major) }}" class="font-semibold text-sky-700">Ubah</a><form method="POST" action="{{ route('admin.majors.destroy', $major) }}" onsubmit="return confirm('Hapus jurusan ini?')">@csrf @method('DELETE')<button class="font-semibold text-rose-700">Hapus</button></form></div></td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-slate-500">Belum ada jurusan.</td></tr>@endforelse</tbody></table></div>
    <div class="mt-5">{{ $majors->links() }}</div>
</x-admin-layout>
